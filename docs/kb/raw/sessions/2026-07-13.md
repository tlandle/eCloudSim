# Session 2026-07-12/13 — scale_out_nsdi rewrite, restructure, naming

Branch (ecav repo): paper-closed-loop-recreate (no ecav code touched this session).
Repo of record: ~/repos/scale_out_nsdi (Overleaf-linked, pushed through d80657b).

## What was done
- Addressed all 21 of Tyler's \tl Overleaf comments on abstract + introduction.
  Iterated live with Tyler through several register corrections:
  - Systems framing, but no basic-concept analogies (database/TCP/cache). New
    memory: feedback_systems_register_not_analogies.
  - "prediction gap" renamed everywhere to handoff-induced cold start /
    cold-start transient.
  - Cooperative prediction not defined as edge-hosted; edge hosting is the
    studied deployment.
  - Planner defined by function (ingests forecasts, makes control decisions),
    not as "the service's client". Frame defined at first use. Kalman mean →
    mean vector + covariance matrix. "Roughly ten frames" universal claim dropped.
  - \tl comments commented out with `|| FIXED:` notes kept in source; \ad/\KR
    (none yet) still get in-text Fixed annotations.
- Review macros (cmds.tex): \ad red, \KR purple, \jordan cyan, \tl blue +
  submission off-switch (from the safety-envelope paper).
- Draft scope: main.tex inputs only abstract/intro/motivation. Other sections
  commented out, files intact. Related work folded into Background and
  Motivation (families as \paragraph, arch refs rephrased, EdgeWarp
  duplication trimmed via commented-out lines).
- New overview figure contents/fig_overview.tex (TikZ) + slides/
  locales_migration.drawio: locales as geographic partitions, single-writer
  ownership boundary, v crossing with A's own forecast as trigger,
  non-connected actor a, edge track tables warm/none, PREPARE prefetch +
  COMMIT on radio attachment, cold-start timeline (~2 s / ~24 m).
- **System named Foresight** (Tyler's pick). Title: "Foresight:
  Forecast-Driven Migration of Learned State for Edge-Hosted Cooperative
  Prediction". Rejected: Predictive Latent Migration, Baton/Torch (race
  metaphors), Courier, Handover. \sys/\Sys macros hold the name.

## Key decisions
- Cold start / cold-start transient is the sanctioned failure name.
- Locale = geographic partition of the service (not "map-anchored", not
  "physical region" alone).
- Only abstract/intro/motivation visible in PDF until other sections mature;
  intro contribution refs to hidden sections commented with restore notes.

## Still open
- MTR stage-1 motion insensitivity (blocks live P0 validation).
- Motivation measurements M1/M4 from Multi-V2X.
- Docker prune reminder (424 GB images).
