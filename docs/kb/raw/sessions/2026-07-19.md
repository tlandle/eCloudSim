# Session 2026-07-16 .. 2026-07-19 — score-head calibration + mode-sweep planner

Branch: paper-closed-loop-recreate

## What happened

Follow-up to the live MTR integration: measured whether the confidence head
actually selects the best mode, then upgraded the planner to consume the
mode set.

## Findings

- Score-head calibration (full test split, 1980 objects): oracle minADE6
  1.45 m vs score-selected ADE 2.33 m; top-1 agreement 62.6%, top-2 79%.
  Selected-ADE p90 5.78 m (tail = diverging-mode cases).
- Live delivered ADE ≈ offline selected-mode ADE → the live pipeline adds
  no degradation; mode selection is the dominant error source.
- Futures are dense (90% full 5 s; GT-sourced), refuting the truncation
  label-noise hypothesis; gap is intrinsic multimodality + thin pasts.
- ecloud.proto GeneratedTrajectory carries only one trajectory, but
  distributed:false runs are in-process, so mode data reaches the planner
  without proto changes.

## Changes

- `behavior_agent.py`: collision loop sweeps top-K modes by score
  (`prediction_mode_top_k`, default 2), earliest conflicting TTC wins;
  drawing re-run uses the conflicting mode; non-multimodal predictors
  unchanged. Precedent: Apollo per-trajectory ST boundaries, Autoware
  confidence-iterated predicted_paths.

## Validation

Smoke run 5: exit 0, zero tracebacks, FDE 0.77-1.20 m / MR 17-23% (same
band as argmax-only), zero ghost brakes / false brakes.

## Next

- Fast-Tesla rerun (mode divergence payoff case; background traffic was
  near-stopped so far).
- AB3DMOT ~9-tick fragmentation fix (deepens pasts → sharper mode scores).
- Live RELAY Q1/Q4/Q5/Q6 arms.
# Session Log — 2026-07-19

**Branch:** `develop`
**Focus:** Phase 1 Step 6 (metrics) + Step 7 (Scenario B) build → live validation → three root-cause discoveries
**Commits:** `df5702e0` (Step 6 metrics), `0886929f` (Scenario B files), `fd3a4e59` (run-5 checkpoint), `64527db0` (real payloads + gated import + coverage-matched locales)

## What happened

### Step 6 — hand-off cost metrics (committed `df5702e0`)
- `ScenarioManager.record_handoff_cost/get_handoff_costs` sink (mirrors `sim_metrics`); pure `summarize_handoff_costs()`; `EvaluationManager.handoff_eval()`; `HAND-OFF MIGRATION` section in `evaluation_report.txt`. Unit tests 8/8.
- **Live gate closed at end of session (run 9):** block present in the real report; `handoff_summary` in `simulation_metrics.json` (1 hand-off, 986 B, serialize 0.0099 ms + network 88.03 ms).

### Step 7 — Scenario B: 9 live CARLA runs, three root causes found

**Naming/geometry:** renamed `left_merge` → `right_merge` (jrapp caught it: CARLA's left-handed frame — heading east, +y is the vehicle's RIGHT). Final scene: ego + stationary ambulance in the LEFTMOST eastbound lane -3 (solid line to shoulder — no left escape), fast NPC in adjacent lane -4 on an explicit `WaypointFollower` plan. A briefly-added 4th "left-lane blocker" vehicle was rejected by jrapp and removed — the leftmost-lane geometry encodes the constraint properly.

**Root cause 1 — merge deadlock (`behavior_agent.py`):** overtake plan lookahead is speed-proportional (`next(speed*6)`); at crawl speed the lane-change plan degenerates to 3-6 m (and `next(0.0)` raises RuntimeError), wedging ego mid-merge forever. Fixed: floored at 15 m (both branches). Also: `SyncArrival` steers 0 → NPC drifted 3.4 m out of lane and parked as a wedge (replaced with plan-driven follower); plan-less `WaypointFollower` stalls (scenario_3's Lincoln pattern = explicit plan required).

**Run 5 = full spec validation (ego path):** ego brakes on edge predictions of the blockage (17 m out — served by edge 1 post-handoff, RSU1 sees the ambulance long before ego's sensors), waits for the NPC to pass, merges right, continues to destination. Geometry-driven handoff for EVERY boundary crossing: managed vehicles → `request_handoff`, obstacles → `transfer_obstacle_state` (pending-retry). Reconfirmed runs 7/8/9. Run 6 looked worse but was mechanically identical — stochastic perception variance + MAX_STEP=500 truncating a slow-approach merge (→ 700). Lesson (jrapp): commit immediately when a run validates.

**Root cause 2 — state transfer was silently empty everywhere (`ab3dmot_state_transfer.py`, new):** `LateFusionEdge` never inherited `_PluggableEdgeBase` — every live handoff (incl. Scenario A's validation) used the base stubs: 98-byte empty payload, no-op import. Extracted `AB3DMOTStateTransferMixin`, wired into LateFusion/WorldFusion/Oracle + pluggable base. Second layer: `KF.carla_id` is -1 for camera detections — anchored identity lives in `track_to_carla` (tid→cid); `_kf_for_carla_id()` resolves through it. Ego payload now real: 986 B. **Warm import gated OFF by default** (`handoff_warm_import`; Phase 1.5 — injecting a snapshot into a tracker that also sees the object risks stale-duplicate ghosts).

**Root cause 3 (OPEN) — obstacle handoff never fires live:** (a) RSU0 has a near-field blind donut — NPC lane detections only at x<20 and x=110-130 (camera geometry at 7 m height); (b) locale boundary must be drawn inside reliable tracking coverage — polygons redrawn 152→115 so the crossing lands in the reacquisition zone; (c) **unresolved:** reacquisition detections (9 dets, ticks 180-188) produce ZERO tracker entries — `n_trk=0` at every retry (`[SCENB-DBG]` diagnostic) — a track-birth failure in `LateFusionEdge`'s detect→track path. **Trace this first next session.** Alternatives also on the table: RSU placement/aiming for continuous coverage; Phase 2 predictive trigger (`Locale.predicted_to_exit_within`).

## State of temp instrumentation
`[EGO-DBG]` + `[SCENB-DBG]` diagnostics remain in `openscenario_multi_edge_right_merge.py` (marked TEMP) — still needed for the obstacle-path work; remove after it validates.

## Environment notes
- CARLA (windowed, DISPLAY=:1) + YOLO gRPC (:18001) were left running at session end.
- Run logs in session scratchpad (`scenb*.log`) — ephemeral.
