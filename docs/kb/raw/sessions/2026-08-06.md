# Session 2026-08-05/06 — both paper scenarios + two-locale blind overtake

Branch: paper-closed-loop-recreate
Commits: 8de40397 (baseline: overtake + right-merge WF), ed1ee4ca
(export identity stamp), 310f9c1e (two-locale blind overtake).

## What was accomplished

1. Blind overtake single-locale COMPLETE (runs 60/61, reproduced):
   staged stop 15.7 m, hold, commit, opposing-lane pass, merge back,
   zero collisions. Defect chain runs 43-61 in current_state.md.
2. Right-merge two-locale on full RELAY stack (runs 1-6): predictive
   NPC latent handoff (440 B memo, 4.1 s advance warning), ego
   ownership handoff, merge clean. Fixes: shared model caches (OOM),
   RSU mast z 7→3 (all-ghost tracks), mamba position-fallback export,
   runner tracker-agnostic diagnostics.
3. Two-locale blind overtake (runs 1-2): warm Leon latent handoffs in
   BOTH directions + ego ownership handoff + clean overtake, zero
   collisions. Fix: sticky locale assignment (overlap precedence bug).

## Key findings

- Edge never tracks its own managed vehicles (anchoring): managed
  handoffs are cold by design; tracker latents are for unmanaged
  obstacles. The paper's migration claim is exactly the obstacle path.
- Firetruck is blacklisted repo-wide (VALID_VEHICLE_TYPES); scenario
  subjects must be whitelisted classes (carlacola swap).
- WF perception managers replaced local detection with an empty stub;
  vehicles now run BOTH pipelines (edge enhances, never replaces) —
  same principle applied at prediction level (edge+local union).
- RSS proper response had two leaks (TTC balloon unhandled state;
  obstacle-less hazard fell through to normal behavior).

## Remaining for the paper

- Distributed-mode variants of both scenarios
- Q1-Q6 + B0-B6 eval sweeps (warm/cold/KF arms via MIGRATION_MODE)
- Locale→compute→physical diagram (needs paper repo path)
- Polish: Leon 201 crossing capture, debug prints → logger.debug,
  TrackerAgnosticMigrationMixin refactor (borrow-list fragility)
