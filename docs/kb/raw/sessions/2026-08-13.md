# Session 2026-08-12/13 (develop)

GT-control day while PACE is in maintenance (back Aug 14; verify:
ssh tlandle3@login-phoenix.pace.gatech.edu).

## Done
- GT-control scenario: openscenario_1_accel_gt (GT injection both edges,
  max_range_m 50 = occlusion-controlled detections). Validated: source tracks
  perfectly, handoff+import correct, gate WAITs when fed the prediction.
- ROOT-CAUSED + FIXED (commit cb9888f4): cadence-dependent velocity units.
  Tracker frames follow detection cadence (WF 0.2 s, GT 0.05 s); downstream
  /edge_dt under-read GT speeds 4x. Wrapper now measures source-tick stride,
  emits m/s (col-13 flag); both trajectory builders honor it. Migration latent
  carries vel_mps; destination dead-reckons in seconds (confirmed ~11.9 m/s
  vs true 12; was 2x slow, caused identity fork at sensing re-entry).
- Creep fix: stopped-lead perceived speed is never ==0; hold at <2 km/h,
  EXCEPT during overtake launch (first fix deadlocked the launch into a
  corner-grind; exemption committed).
- Figures to Tyler: first set was diagnostic (wrong altitude, Tyler called it);
  redone as PAPER mockups: P1 locale-sizing vs safety (4 arms), P2 protocol
  timeline at boundary (predictive/reactive/cold, 776 B, 1 s lead), P3 scale
  envelope (max entry speed vs separation). All PROJECTION-labeled, in
  scratchpad figs/ + make_paper_figs.py.

## Open (next session)
1. Accel-profile GT pair (ONCOMING_ACCEL=1): BOTH arms grind the truck ~880
   contact ticks and the gate never sees the oncoming (npreds 1-2). Two
   suspects: overtake launch still stalls near the stopped truck (launch-fix
   rerun showed same grind — verify fix was active in gtf2 logs); and the
   destination never broadcasts the slow-cruise (5 m/s) oncoming's prediction
   under the accel profile (worked at constant 12 in gtc4). Diagnose with
   $SB/gtf2_warm.log first: EGO-DBG around first collision + MTR IN/CADENCE.
2. Constant-speed GT pair (no accel): rerun warm/kf with creep+launch fix;
   previous best gtc4 warm had only the truck-grind collision left.
3. PACE returns Aug 14: copy /home/atlas/Multi-V2X.tar.gz (235 GB) to scratch,
   launch car-only + translation-aug finetune (45 ep, config in
   worldfusion_multiv2x_caronly_aug_thresh02/config.yaml{,.pace}); target
   high recall at low confidence AND low FP. Consider new close-range
   oncoming training data from the sim (Multi-V2X-format PCD+GT generator,
   not built yet).
4. Seed-sweep result stands: warm NOT better than kf on noisy WF; retrain is
   on the critical path for the thesis.
