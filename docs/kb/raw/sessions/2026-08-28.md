# Session 2026-08-28: overlap argument written, design-study campaign launched

Branch: develop. Repos touched: ~/repos/dissertation, ~/repos/scale_out_nsdi,
this sandbox.

## Dissertation writing

- New subsection 6.1.1 "Why Not Overlapping Locales": four-design taxonomy
  (sensing overlap required / input forwarding complementary / mirroring a
  rate on Khonsu's own axis / dual-service the real competitor), three
  measured costs, partial-warmth bound (rebuild window x speed = 30-40 m
  of dual-served road per boundary vs one 1.3 KB send), make-before-break
  already in the protocol, disconnected-locales sibling answer. NSDI intro
  got a two-line forward of the same argument.
- Trigger subsection: geofence-band alternative analyzed honestly (it IS
  migration, not overlap); predictive wins on lead scaling (w/v),
  selectivity, and zero added mechanism; trigger promoted to a measured
  axis (at-crossing -> bands -> forecast).
- Protocol subsection retitled "Transfer, Ownership Handshake, and
  Correctness Contract": 1.3 KB needs one send, ownership needs two steps,
  commit refresh (delta/full/none) is a measured question. "Two-phase" was
  inherited VM vocabulary and Tyler's question exposed it.
- De-jargon pass on my own additions after Tyler called it out (prepare
  phase forward-ref, rate knob, MEC-representative, tuning surface,
  make-before-break defined at first use, excess "exactly"). Rules in
  memory feedback_writing_simply now gate my drafts too.

## Sandbox: design-study harness (commit 9d5e1883)

- openscenario_1_flow_gt.py new knobs: TRIGGER_MODE=band + BAND_W_M
  (distance measured to the DESTINATION locale; first smoke proved the
  own-polygon version fires on lateral edges, an NPC 79 m from the
  crossing fired at W=20, live evidence of band over-firing),
  COMMIT_REFRESH=full (re-send at actual crossing), MIRROR_PERIOD_S
  (periodic re-send), and a [RUNROW] teardown line (gap-2 deduped
  collision episodes from the ego collision-sensor history, contact
  ticks, transfers, bytes) so runs are self-describing and the
  committee's binary collided metric falls out directly.
- scripts/khonsu_design_sweep.sh (resumable) + khonsu_design_extract.py.
- Smoke verified: band fires only within W of the crossing; refresh fires
  at the crossing; RUNROW + extractor produce a correct CSV row. Exit 134
  at teardown is the known non-fatal CARLA abort.
- CAMPAIGN LAUNCHED: evaluation_outputs/khonsu_design_sweep_v1, 12 configs
  x 10 reps (~120 runs, ~10 h): S1 trigger axis (warm, reactive, band
  5/10/20/40/80), S2 refresh, S3 mirror 0.25/0.5/1/2 s, S4 kf/edgewarp/
  cold top-up. Plan: docs/agent_plans/khonsu_design_studies.md.
  NOTE: dual-service overlap arm intentionally not built; in this GT
  harness the destination already observes across the boundary by range,
  so cold IS rebuild-from-own-observation and bounds overlap-dwell
  warming; report cold's observability window instead.
