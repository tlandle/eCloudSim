# Session 2026-08-31: campaign complete; canonical flow table is not reproducible

## Campaign result (150/150 runs, zero warnings, uniform stack)

Rows: docs/kb/data/relay_eval_2026_08/design_sweep_v1_rows.csv.
Summary at 10 reps/config: every arm collides 10/10 (collision-sensor
buffer saturates at its 30-entry cap; one sustained grind at the truck,
median 50 s timeout at ~67 m). Completion: warm(1.0s lookahead) 1/10,
band5 1/10, band10 5/10, band20 6/10, band40 8/10, band80 6/10, reactive
(fixed, real at-crossing transfers) 3/10, refresh 2/10, mirrors 1-2/10,
kf 5/10, edgewarp 1/10, cold 1/10.

Internally valid signals:
- Lead time drives completion monotonically (band width proxy). Warm's
  1.0 s default lookahead starves the forecast trigger; the trigger-axis
  comparison needs warm at lookahead 2/3/4 s to be lead-matched.
- Wider bands over-fire (2.8 transfers vs warm 2.3): selectivity cost
  measured.
- Fixed reactive (3/10) beats cold (1/10): at-crossing transfer helps.

## The provenance finding (serious)

The canonical flow table (q4_flow_table.csv, commit ecc4b092, 08-20;
warm 4/4 clean complete 21-29 s) is NOT reproducible today. Bisection:
- old planner (pre-RSS trio) + current rest: grinds.
- old GT injection + current rest: grinds.
- ALL of ecav/core at ecc4b092: grinds (3/3).
- fully canonical code incl. scenario file: rep1 grinds 58 m, rep2
  93.9 m but 49 s (canonical was 21-29 s).
Also checked: scenario XML unchanged, YAML unchanged, checkpoints
untouched since July, long-lived vs fresh CARLA both grind today.
Conclusion: the difference is environmental (machine/CARLA state since
08-20) or the canonical runs came from an uncommitted working state.
The 08-16 raw logs (eval_flow_v2.csv etc.) no longer exist, so
tick-level forensics against them is impossible.

ALSO discovered en route: reactive/edgewarp NEVER fired transfers in
flow_gt (zero-horizon predicted_to_exit_within cannot return true), in
the canonical table too. Their canonical rows are cold-with-other-seeds.
Fixed in 4ccd5906 (at-crossing containment test).

## Implications (for Tyler's decision)

1. The canonical flow table cannot back the NSDI/dissertation headline:
   two of its arms never transferred, and its absolute numbers do not
   reproduce from their own commit.
2. The scenario outcome is dominated by environment-sensitive state
   (oncoming gap structure vs world clock / light phase suspected).
   Paper-grade numbers need the scenario hardened for determinism
   (pin traffic-light state, decouple the oncoming stream from world
   clock, log the gap structure per run) and then a full re-baseline.
3. The design-sweep harness is ready and resumable; once the scenario
   is hardened, one overnight reruns everything with warm at matched
   lookaheads {1,2,3,4} added.

Working tree restored to HEAD (knobs + fixes intact).

## Progress deck + overlap argument finalized (same day)

- Six slides appended to gtvault "eCloud Progress.pptx" (now 357): why
  Kalman snapshot fails (accel 2x2, clean-completion metric), microbench
  mechanism, why transfer must land ahead (lead-time curve from the new
  sweep), why EdgeWarp needs two phases and we need two steps (refresh/
  mirror bytes-vs-outcome), forecast-vs-band trigger (over-firing), and
  the overlap decomposition table. Tyler edited the first five live
  (retitles, trims); his edits preserved, slide six added on top of his
  copy, pushed byte-verified.
- Overlap argument REFRAMED (Tyler pushback: "you still don't explain
  why not overlap+trigger or overlap+replication"): every boundary
  scheme answers two questions, delivery and authority. Overlap-entry
  trigger IS migration (measured, worse trigger). Replication-then-
  switch is the prepare phase at a higher rate (mirror arms: bytes up,
  outcomes flat; cost scales with boundary-dwelling traffic not
  crossings). Replication-without-switch is dual authority and
  reimports the measured multi-source failure at forecast level.
  Dissertation 6.1.1 rewritten around this decomposition.

## Story deck built for Kishore (khonsu_story.pptx on gtvault, 13 slides)

- NEW measurement: canvas-size fusion latency sweep
  (scripts/canvas_latency_bench.py, canvas_latency.csv). Fusion alone
  crosses the 130 ms SLO between 282-400 m canvas at N=8 (90->193 ms);
  quadratic-in-side growth; memory never binds (6.8 GB peak at 563 m).
  Practical locale cap ~300 m once tracking+prediction take their share.
- Deck structure per Kishore's ask: SQ1-SQ5 study questions up front,
  every slide tagged and tied to its evaluation. SQ1 canvas cap + Q7
  boundary geometry; SQ2 KF-vs-SSM (maneuvering-target literature +
  microbench + accel closed loop; first-SSM-3D-in-stack claim); SQ3
  conflicts anywhere (NHTSA typology + measured baseline losses); SQ4
  envelope-across-handoff age chain (new figure; chain visibly exceeds
  the 220 ms tightest budget, motivating the computed trigger) +
  migrate-not-overlap; SQ5 knobs table + question-to-evidence map;
  paper scoping slide (in/out).
- Contribution reframe (Tyler pushback session): migration as deadline
  scheduling across two services. Invariant: age at use within the
  maneuver budget through the ownership switch. Computed trigger from
  destination refresh queue + budget; burst admission; feasible
  crossing-load region; boundary placement rule. Memory updated.
