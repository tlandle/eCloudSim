# Session 2026-09-07 (writing session; eval session in parallel)

Branch: develop (sandbox), main (scale_out_nsdi). Professor meeting at 11:00.

## What was touched

- Deck (`scale_out_nsdi/slides/khonsu_story.pptx`, builder `build_khonsu_talk.py`): rebuilt as a talk deck (point titles, three fragment bullets, one visual, RQ and status tags, notes carry detail); Office image fix by LibreOffice re-save; Tyler's PowerPoint comments carried by `transplant_comments.py`; reviewer scaffold order (23 slides + Takeaway + backup); notes regenerated at build time from `contents/gen_numbers.tex`; figures re-synced from the paper's floats by `sync_paper_figs.py`. Pushes to OneDrive stopped after the overwrite complaint; delivered by attachment only.
- Paper (`scale_out_nsdi`): reviewer passes 3 to 9 applied by a fork: §5 restructured on the ladder (one locale, two locales, T_avail regions, freshness, trigger, alternatives, corridor, scale, sizing); projected working draft with 19 generated CSVs in lander schemas (`mock_data/`), `scripts/plot_figures.py` (figures, tables, 173+ macros), `scripts/figure_rules.py` (225/225), `docs/figure_schemas.md` (landing contract), `docs/figure_status.md`; abstract and intro line pass; data audit (freshness on total age, EdgeWarp full record, repl_final, capacity split, single-source density); §3 freshness budget stated as edge plans, vehicle enforces, overruns observed; §4 per-frame source ticks. 25 pages, untrimmed by design.
- Sandbox code (eval session): freeze-1i quarantined (planner abort path); freeze-1j (planner reverted to 1h); freeze-1k = planner per-tick recheck with a safe response + snapshot velocity export + CV forecast for immature imports + per-frame source ticks + exact-tick velocity and coast + log-only dual-compute counters; tag 4d94d420 (object bc26cd3e). Campaign split: Atlas A, B, F, E, A 11-20, B 11-20; cetus ACC, T25, Table 8 @10, visible matrix, netem, bisect. freeze-1l (override fix, repl_final, kf_final, shift knob, logging, FAULT_MODE) in build.
- Data landed on 1j (superseded by 1k): A, B, E, F (205 rows), cetus T12 bo, N=28, Table 8, loaded N=20 (invalid: override defect).
- SEC short paper: arXiv:2609.04364 in the bib; Conductor arXiv id recorded.

## Key decisions

- Fix and rerun on every defect (1i abort path, 1k planner recheck, snapshot straw man, estimator cadence); one tag per table.
- Kalman baseline must be fair: position + velocity + CV forecast from the first frame; the two-locale constant-speed rung says a velocity snapshot suffices when time allows.
- T_avail (time available from crossing to conflict) is the axis; decision time is an outcome; speed and geometry generate points.
- Deck shows the paper's projected results with provenance in notes; measured interim results in backup; never a measured label on a projection.
- OneDrive: no pushes while Tyler may be editing; version history checked before any push.

## Open

- 1k landings (headline ~02:00 Sep 8, then trigger, F, E; cetus ACC, T25); 1l smoke and blocks; hist5 dip diagnosis; burst/density at 10 seeds; page trim after data lands.
