# Session 2026-09-09 (writing session; eval session in parallel)

Branch: develop (sandbox), main (scale_out_nsdi). Tyler is editing the paper text with Gemini (low on credits); paper prose held on this side until his Overleaf sync; "focus on evals here".

## What landed

- T25 speed sweep rerun (freeze-1k, 200 rows): cold vs migrated separation, strongest at low speed (cold collides 10/10 at 8 and 12 m/s), every arm safe at 16 m/s and above; Khonsu vs Kalman one run in fifty; Kalman 7/10 at 24 m/s (three never launched). Cold's transition is inverted against the generated panel.
- Accel sweep on the old gate (freeze-1l, 250 rows): every arm 10/10 at every level; est_a tracks GT; commanded 3 to 6 plateau at 6.3 m/s^2 (speed cap). Root cause: the overtake gate reads current position and tracker speed only; the forecast reaches only the emergency brake.
- Loaded headline hln31/hln20 (freeze-1l, 70 rows each): every migrating arm 9 to 10 of 10 at both counts; cold 4/10 at 20 and 2/10 at 31; realized age at the decision ~250/600 ms at 20 and ~375/1000 at 31. The projected reactive separation at 31 does not exist (the decision is ~2 s after commit at this geometry).
- FDE subset (freeze-1l, 30 rows, re-landed with the window anchor): 5 s FDE EdgeWarp 40.6, Khonsu 42.0, reactive 44.9, cold 45.4, handover 45.6, Kalman 58.5 m; 3 s Khonsu 26.6 vs Kalman 44.1. The predictor's forecast is stop-biased on this scene.

## Decisions

- Tyler: option 1 (freeze-1m = gate change, rerun accel + T25, other blocks stay on their tags).
- Gate v1 (walk MTR's full 5 s forecast): smoke adverse (no meeting within 5 s; CV fallback; the endpoint slope would have driven the ego into the oncoming). Not adopted.
- Gate v2 (writing session, under Tyler's P0): the kinematic forecast of the delivered track (closed form, a = migrated est_a / local estimate / 0), MTR keeps the crossing-conflict check; reduces exactly to the old gate at constant speed.
- Maneuvering panel redesigned as a T_avail sweep at commanded 2 via ONCOMING_SHIFT_X (implemented today; probe on Atlas), arms warm, warm accel-off, kf, reactive, cold x10; constant panel = same shifts at 12 m/s (T25b) + T25; accel-magnitude sweep dropped as the panel.
- T_avail redefined per shift as the focal CAV's arrival at the decision point minus the oncoming's crossing (arm-independent); launch minus crossing is the annotation.
- Non-central chain: T25b first, then freshness (accel level from the new sweep), then the rest; held at the epoch_smoke_ok gate.
- Cold FDE anchored at the destination's first own forecast of the gating oncoming.

## Open

- Probe mapping (shift -> T_avail, destination frames at the launch) for sign-off; smoke (shortest + 1.0 s shifts, warm/kf; flow byte-identical); reruns on freeze-1m.
- Cetus tail -> corridor smoke (epoch fence) -> corridor campaign (70 routes incl. warm EPOCH_FENCE=0).
- Paper landings behind Tyler's sync: loaded blocks, FDE table, T25 constant panel, §5.3 definition, loaded and record-ablation wording, abstract rewrite.
