# 2026-09-09 (code session)

Branch: develop (shared checkout with the paper/eval peer session). Explicit-pathspec commits only.

## Summary

The accel maneuvering panel was diagnosed to its root cause and the fix built. The
closed-loop separation the paper needs is cold-vs-migrated (robust); fresh-vs-stale
(warm vs kf) does not appear in closed-loop collisions because the overtake gate is
forecast-horizon-blind. Tyler chose to fix the gate (option 1). freeze-1m built and
smoke-deployed; no rerun launched yet.

## What was touched / decided

- Accel a=1..5 landed (frozen1l_acc_rows.csv, 250 rows, commit f2718cc2). est_a fix held.
  Collision metric saturated (0/250, all arms). GT accel PLATEAUS: commanded 2->3.9,
  3..6->6.3 (speed cap); the 5-level sweep is really 2 physical levels.
- T25 speed-sweep rerun landed (frozen1k_t25_rows.csv, 200 rows, commit 4e2d1959).
  Cold-vs-migrated at low speed (v8/v12 cold 10 collisions); warm-vs-kf does not
  separate on collisions; kf v24 3 timeouts (a metric separation once timeout=failure).
- Peer metric correction: completed-without-collision is the metric; timeout/unfinished
  = failure, not neutral. Produced three outcome tables (scripts/khonsu_ablation... no:
  scratchpad/peer_tables.py): T25 success/collide/timeout/never-launch; accel mechanism
  (launch_tick, aborts, holds, recommit, backstop ttc<0.5, comp_time); GT accel per level.
- ROOT CAUSE CONFIRMED (commit ed2feb4a): the overtake go/no-go is forecast-blind.
  _nearest_oncoming_ahead (behavior_agent_1k.py:1092-1168) uses traj[0] (current pos) +
  kf_speed_mps (current speed); need=4*(7+kf_speed_mps). Consumers: initial gate :2263,
  recheck :1765, recommit :1813. The forecast horizon feeds ONLY the trajectory_collision
  backstop (:845-879, top-2 modes), not the decision. So warm=kf by construction; cold
  differs only because it has no track. Fresh-vs-stale is an FDE effect, not closed-loop.
- Tyler chose option 1 (fix the gate). freeze-1m built:
  - mtr_edge_predictor_1m.py: predicted_trajectory_full = full 5s horizon (25 MTR steps
    at 0.2s, world) as a Python attr (in-process, no proto) on the mature-MTR pred and
    the immature-import pred (constant-accel to 5s); full_dt per pred. Backstop's 1.25s
    predicted_trajectory + num_output_steps untouched.
  - behavior_agent_1m.py = behavior_agent_1k_epoch + gate change (patch-merged; epoch
    blocks at __init__/823 do not touch the gate region). _nearest_oncoming_ahead returns
    (clear, speed, arrival); _gate_arrival walks predicted_trajectory_full to the ego's
    advancing overtake front (meeting = first t with onc_ahead <= 7*t, interpolated);
    fallback distance/current-speed beyond 5s or no forecast. Decision = arrival >= 4s at
    all three sites. endpoint-extrapolation kept as a LOGGED column only. [GATE ARR] logs
    cv/full_walk/endpoint/src/arrival + cid per decision tick. Constant speed: walk==CV
    (byte-identical, smoke 1 verifies).
- Design fork resolved with the peer: NOT endpoint-slope of the 1.25s truncation (a proxy);
  emit the model's actual 5s forecast as a gate-only field. Integrity rule: report whatever
  the real MTR 5s forecast implies even if warm looks worse (predictor finding), do not
  cherry-pick the extrapolation variant for the expected sign. Read decision_path first.
- Accel redesign levels (Tyler/peer): commanded 1, 1.5, 2, 2.5, 3 (GT ~2.5-4.5 then 6.3).
- Chain change: acc05 (a=0.5, old gate) killed; _acc05_done written as a sequencing marker;
  FDE subset advancing on freeze-1l (gate-independent, valid). Non-central held (no
  epoch_smoke_ok) so freeze-1m accel+T25 go first.

## Runs / state at session end

- Atlas: central (hln) -> FDE subset (freeze-1l) -> non-central (held). idfix_wt @ 71c9f37e.
- Cetus: 1k tail (t8/visible/netem) running; cetus_1m_smoke.sh queued behind it (flock),
  auto-runs on tail completion. freeze-1m overlay staged to /tmp/m_stage (14 files).
- Watchers: bvnw3o5z8 (tail done -> land t8/visible/netem CSVs), bfoj5wqwb (1m smoke done
  -> read arrivals + decision_path + byte-identical, decide rerun).

## Files changed (committed)

- docs/kb/data/relay_eval_2026_08/frozen1l_acc_rows.csv (f2718cc2)
- docs/kb/data/relay_eval_2026_08/frozen1k_t25_rows.csv (4e2d1959)
- docs/kb/wiki/current_state.md (f2718cc2, 4e2d1959, 952a85a3, ed2feb4a)

## Scratchpad (freeze-1m, uncommitted overlay)

behavior_agent_1m.py, mtr_edge_predictor_1m.py, cetus_1m_smoke.sh, peer_tables.py.

## Next

1. Read the 1m smoke: decision_path @ commanded 2, then cv/full_walk/endpoint arrivals,
   warm-vs-kf launch, flow 1m-vs-1l byte-identical. Report BEFORE any rerun.
2. If the gate discriminates correctly: freeze-1m accel (levels 1/1.5/2/2.5/3) + T25 rerun
   split Atlas+cetus, land frozen1m_acc_rows.csv + frozen1m_t25_rows.csv. Set trigger
   distance from the smoke. Then non-central (freshness first, new accel level).
3. If the mature MTR forecast is adverse (implies later meeting under accel): report as a
   predictor finding, do not proceed to rerun, escalate to peer/Tyler.
