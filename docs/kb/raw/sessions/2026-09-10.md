# Session 2026-09-10 (writing session; eval session in parallel on uds 4031)

Branches: develop (sandbox), main (scale_out_nsdi). NSDI paper due Sep 17.

## What was touched

- **Paper conflict figure** (2b51c51 -> 8510b9b): Tyler, "the graph where we show the number of
  conflicts, we need to show vehicle density here, otherwise the point is moot". Rebuilt as two
  panels: distance-to-boundary CDF with one curve per traffic level, and boundary-adjacent conflicts
  per km per hour against traffic level with the fixed conflicts flat. My verification caught two
  things the fork had not: exposure was deduplicated by VALUE not by map (correct only because the
  three towns happen to have distinct road lengths), and the rate panel was a pooled line with no
  scatter when the per-map spread was real. Also closed a 67 px legend gap, dropped the left panel to
  three levels so the curves separate in black and white, and renamed "traffic density" to "traffic
  level" because crossings per minute is a flow, not a density.
- **Deck**: problem slide rebuilt front and centre with a purpose-drawn schematic (a52240f); Tyler's
  two OneDrive edits ported before rebuilding; an unresolved f-string that printed a raw macro on a
  slide fixed; closed-loop arm renamed off "Kalman" in nine places. Pushed and byte-verified twice.
- **Baseline rename** (paper ac02a93, deck f0f4cf7): cold start -> no state transfer, Kalman snapshot
  -> velocity snapshot, handover snapshot -> radio-handover snapshot, reactive transfer -> history at
  crossing, ground-truth crossing trigger -> ground-truth trigger. 41 prose edits, 20 floats
  regenerated, 67 deck replacements, deck figure generators re-rendered.
- **Limitations** (b342bbf): new paragraph answering the advisor's open question about sparse
  roadside deployment.
- **Reproducibility**: freeze_1m_overlay pushed (it had been LOCAL ONLY); apply.sh hazards fixed;
  gen_kf_vs_ssm.py committed (2ca8fb46); corridor block committed (f1265216) plus its top-level
  runner (56d50c20); models pinned by content hash (57af285a) with the guard in both runners
  (3452f375).
- **Campaign**: launched on two hosts. Regions top on atlas, corridor on cetus, both landing into the
  paper repo. Ten-minute verifier running.

## Key decisions

- The closed-loop maneuvering panel was NOT load-bearing; the record-depth claim rests on the
  closed-loop record ablation and the offline forced-handoff benchmark, and evaluation.tex already
  says the knee is at two frames. I had treated it as load-bearing and cost the eval session four
  probes. Tyler stopped it: "you can't get things to actually vary, which is surprising, but not a
  result."
- Acceleration block restarted per Tyler on REALISTIC DRIVING (slow lead vehicle, car-following
  brakes then accelerates: the §2.3 queue-tail case) rather than a hand-placed acceleration event,
  and worked IN TANDEM with regions because the regions figure's second panel IS the acceleration
  block.
- Regions sweeps T_avail by VEHICLE SPEED at fixed geometry (6-24 m/s), not by shift. The eval
  session's notes had a shift sweep, which would have made evaluation.tex wrong and the figure's
  secondary axis meaningless.
- FDE stays its own block (MODESROW plus a no-handoff reference arm). I withdrew my instruction to
  fold it into the T25 collision runs: a second, differently-computed FDE is worse than none.
- Ten seeds per point, ordered five-first across the whole grid so an interrupted campaign leaves a
  complete coarse figure rather than a half-finished fine one.

## Errors I made

- Repeated the eval session's 3-day runtime estimate without checking it. Tyler challenged it;
  measurement showed 294 runs/day/host sustained, about 4x faster, and the full 1372-run campaign
  fits on two hosts by ~Sep 13.
- Wrote the campaign verifier with my own reimplementation of "completed without a collision" that
  ignored the collision column, so every generated run looked like a success. Now imports success()
  and arm_of() from figure_rules.py.
- Declared the maneuvering panel unachievable when only one configuration had been tested.

## Open

- Regions bottom (realistic-driving profile) to validate on one cell, then sweep.
- Five pending runners: controlled-age, overlap-width, trigger-Pareto, alternatives,
  import-age/faithful-EdgeWarp.
- Post-deadline: per-fix merge of freeze_1m_overlay into the base source files.
- Page trim to 12 after the data freeze.
